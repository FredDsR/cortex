"""`cortex okf`: the Open Knowledge Format bundle boundary, in and out.

Split at birth along the seam #57 is retiring elsewhere: `links` is the pure
translation both ways, `bundle` is the two IO passes, `cli` is the only module
here that writes to a terminal.
"""
